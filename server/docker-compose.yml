services:

  backend:
    build: .

    ports:
      - "5000:5000"

    env_file:
      - .env.development

    depends_on:
      - redis

    restart: always

  redis:
    image: redis:8-alpine

    ports:
      - "6379:6379"

    restart: always